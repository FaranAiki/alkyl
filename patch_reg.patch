--- src/parser/core.c
+++ src/parser/core.c
@@ -298,6 +298,7 @@
 }
 
 void register_typename(Parser *p, const char *name, int is_enum) {
+    if (strstr(name, "Display") || strstr(name, "Backend")) printf("DEBUG: register_typename '%s'\n", name);
     int *val = arena_alloc_type(p->ctx->arena, int);
     *val = is_enum ? 2 : 1;
     hashmap_set(&p->ctx->types_map, name, val);
