{
    "keyword": "string",
    "description": "A built-in string type. String literals like `c\"Hello\"` are typed as `char*` but can be coerced to `string`.",
    "features": [
        "Interchangeable with `char*` in many contexts.",
        "High-level text processing."
    ],
    "example": "let name: string = c\"Alkyl\";",
    "constraints": []
}