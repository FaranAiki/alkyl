{
    "keyword": "bool",
    "description": "A primitive boolean type representing `true` or `false`.",
    "features": [
        "Logical operations (&&, ||, !)."
    ],
    "example": "let is_valid: bool = true;",
    "constraints": [
        "Cannot be implicitly cast from/to integer types without explicit conversion in strict contexts."
    ]
}