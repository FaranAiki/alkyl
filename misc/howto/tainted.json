{
    "keyword": "tainted",
    "description": "Marks a variable or function as potentially dangerous or originating from an untrusted source. In Alkyl, any function marked as `extern` inherently returns `tainted` and is considered `impure`.",
    "features": [
        "Data flow tracking for security and safety.",
        "Must be explicitly handled via `wash` or `clean` before it can be used safely."
    ],
    "example": "extern int get_user_input(); // Inherently returns tainted\n\ntainted int t = get_user_input();\n\nwash t as err {\n    // handle error\n} then {\n    // safely use t\n}",
    "constraints": [
        "Cannot be passed directly to contexts where `pristine` data is required."
    ]
}