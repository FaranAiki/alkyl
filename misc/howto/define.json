{
    "keyword": "define",
    "description": "Defines a constant alias or macro using the `define X as Y` syntax. This acts like C's `#define` but integrates with Alkyl's syntax.",
    "features": [
        "Constant replacement at compile time.",
        "Clear syntax: `define NAME as VALUE`"
    ],
    "example": "define MAX_SIZE as 1024\nlet array[MAX_SIZE];",
    "constraints": []
}
