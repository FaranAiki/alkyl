const single pi = 3.14;

pure single tan(pristine single n) { return n; }

int fun(int a) {
    return tan(pi / a); // Should fail with taint error and hint
}

int fun2() {
    return tan(pi / 2.0); // Should pass
}
