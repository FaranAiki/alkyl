namespace fnt {

// Fix this because Type(Type, Type) should be possible as a type of a function!
compound [type Type]
Type reduce(Type(Type, Type) fn, Type[] list) {
  Type rt = list[0];
  for i in (sizeof(fn)/sizeof(Type)) - 1 {
    rt = fn(list[i], rt);
  }
  return rt;
}

}
