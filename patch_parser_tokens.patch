--- src/parser/core.c
+++ src/parser/core.c
@@ -1071,6 +1071,11 @@
               p->token_capacity = new_cap;
           }
           p->tokens[p->token_count++] = t;
+          if (t.line >= 332 && t.line <= 335) {
+              printf("PARSE_PROGRAM lexed token: type=%d text=%s line=%d col=%d\n", t.type, t.text ? t.text : "", t.line, t.col);
+          }
           if (t.type == TOKEN_EOF) break;
       }
   }
