import "lib/std/heap.aky";

void main() {
    heap.ArenaAllocator[int]* arena = heap.ArenaAllocator[int]();
    int* ptr = arena.alloc(2);
    ptr[0] = 42;
    ptr[1] = 43;
    arena.free();
    printf(c"Test 1 OK\n");
}
