extern int printf(char*, ...);

class Tester {
    compound[type T]
    public T* alloc(long count) {
        return 0 as T*;
    }
}
int main() {
    let t = Tester();
    let ptr = t.alloc[int](5);
    return 0;
}
