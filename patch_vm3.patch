--- src/metalir/vm.c
+++ src/metalir/vm.c
@@ -107,6 +107,7 @@
         AlirInst *inst = curr_block->head;
         while (inst) {
 
+            printf("EXECUTING INST OP %d in func %s\n", inst->op, func->name);
             VMContext ctx = {
                 .vm = vm,
                 .module = module,
