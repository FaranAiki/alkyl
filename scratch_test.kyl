import "std/print";

errnum[ErrThree]
tainted bool hasError(int a) {
    purge ErrThree;
}

int main() {
    let z = hasError(3);
    let r1 = z ?[ErrThree] false;
    let r2 = r1 ? true;
    if (r2) {
        std.print "True!";
    } else {
        std.print "False!";
    }
}
