#include <stdio.h>
#include "include/parser/typestruct.h"
#include "include/lexer/lexer.h"
int main() {
    printf("TYPE_UNKNOWN=%d\n", TYPE_UNKNOWN);
    printf("TYPE_CLASS=%d\n", TYPE_CLASS);
    printf("TOKEN_LBRACE=%d\n", TOKEN_LBRACE);
    return 0;
}
