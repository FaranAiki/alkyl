import "std/input";

// TODO this is still broken!
int main() {
    tainted int t = 1;
    std.input(&t);

    if (t ? -1) != 1 then return 1; // if the user did not press 1 OR there is an error, return 1;

    std.input(&t);
    if (t ? -1) != 2 then return 2; // if the user did not press 1 OR there is an error, return 1;

    return 0;
}
