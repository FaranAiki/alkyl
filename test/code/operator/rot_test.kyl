extern int printf(char *fmt, ...);
int main() {
    int x = 1;
    printf("rotr = %d (should be negative -something)\n", x %>> 1);
    printf("rotl = %d (should be 2)\n", x <<% 1);
    return 0;
}
