extern int printf(char* format, ...);

errnum [ErrNull]
int m?() {
  purge ErrNull;
  return 0;
}

int main() {
    tainted int x = m();

    clean x {
        printf(c"Clean\n");
    } residue err {
        switch (err) {
            case ErrDivisionByZero:
                printf(c"Div by zero\n");
            case ErrNull:
                printf(c"Null error\n");
        }
    }

    return 0;
}
