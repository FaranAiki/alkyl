extern int printf(char *s, ...) as print;

errnum[ErrDivByZero]
int divbytwo?(int a, int b = 2) {
  if b == 0 then purge ErrDivByZero;
  return a / b;
}

int main() {
  tainted int t = divbytwo(100, 0);
  untaint t residue ErrDivByZero {
    print "Hmm... something is wrong!";
  }
}
