// AI-LLM: This was already implemented in parse_extern()
extern {
  void* malloc(int size) as cmalloc;
  void* free(void*) as cfree;
}

int main() {
    void* p = cmalloc(10);
    // this should compile fine
    cfree(p);
    return 0;
}
