import std.print
class A {
    B* b;
    int x;
}

class B {
    A* a;
    int y;
}

int main() {
    let mut b = B(0 as A*, 20); // wait, 0 as A*?
    let mut a = A(&b, 10);
    print (a.x);
  return 0;
}
