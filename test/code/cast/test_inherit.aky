import std.print
class A is B {
    int x;
}

class B {
    int y;
}

int main() {
    let mut a = A(0, 0); // Implicit constructor args?
    a.y = 1;
    a.x = 2;
    print (a.x + a.y);
  return 0;
}
