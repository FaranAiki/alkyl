extern int printf(char*, ...);
compound[type[int] Type] {
    class Vector {
        Type x, y;
    }
}
int main() {
    let a = Vector[int](1, 2);
    mut Vector[int] b(a);
    a.x = 99;
    printf c"b.x = %d, b.y = %d\n", b.x, b.y;
}
