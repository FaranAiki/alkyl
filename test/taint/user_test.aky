extern int scanf(string, void*);
extern int printf(string, ...);

pristine int int_input() {
    tainted int tmp;
    scanf("%d", &tmp);

    clean tmp {
      return tmp;
    } residue {
      purge "Not a valid number!";
    }
}

int main() {
    printf "Pilih dua angka yang ingin dibagi:\n";
    int enumerator = int_input();
    int denumerator = int_input();
    tainted double hasil = (enumerator / denumerator) as double;
    clean hasil {
      printf "Hasilnya %lf\n", hasil;
    } residue {
      purge "Ngga boleh dibagi 0!\n";
    }
}
