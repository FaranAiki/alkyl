extern int printf(char* fmt);

premeta {
    compiler.no_purge = true
}

int main() {
    reason "just because" int x = 5;
    
    // Test nested reason
    {
        reason "testing block reason"
        int y = 10;
        printf("Hello World\n");
    }
    
    return 0;
}
