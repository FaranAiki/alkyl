pristine int add(int x) {
    return x + 1;
}

int main() {
    tainted int t = 0;
    
    wash t as err {
        // error handling
    } else {
        // Here t should be pristine!
        add(t); // Should pass
    }

    // Outside else block, wash leaves t tainted
    // add(t); // would fail here!
    
    untaint t as err {
        // error handling
        return 1;
    }
    
    // After untaint, t is pristine!
    add(t); // Should pass

    return 0;
}
