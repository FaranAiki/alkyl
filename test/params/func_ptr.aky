extern int printf(char *s, ...) as print;

int(int) t(int p) {
  // wait we need to return a function... how do we define an inline function?
  // wait, let's just use a function name
  return 0; // wait, returning 0 as function pointer?
}

int main() {
    return 0;
}
