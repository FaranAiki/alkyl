0a1,7
> Both operands to ICmp instruction are not of the same type!
>   %eq = icmp eq i64 ptrtoint (ptr getelementptr (ptr, ptr null, i32 1) to i64), i32 1
> Both operands to ICmp instruction are not of the same type!
>   %eq = icmp eq i64 ptrtoint (ptr getelementptr (ptr, ptr null, i32 1) to i64), i32 1
> LLVM Verification Error: Both operands to ICmp instruction are not of the same type!
>   %eq = icmp eq i64 ptrtoint (ptr getelementptr (ptr, ptr null, i32 1) to i64), i32 1
> 
7,9d13
< Compiled to build/out.o
< Linking: gcc -g -O0 build/out.o -o build/out -no-pie 
< Linked to build/out
