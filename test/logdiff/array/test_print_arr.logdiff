1,43c1,2
< Both operands to ICmp instruction are not of the same type!
<   %eq = icmp eq i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 1
< Both operands to a binary operator are not of the same type!
<   %shr = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 2
< Both operands to a binary operator are not of the same type!
<   %shr54 = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 2
< Both operands to a binary operator are not of the same type!
<   %shr59 = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 3
< Both operands to a binary operator are not of the same type!
<   %shr64 = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 3
< Both operands to a binary operator are not of the same type!
<   %shr69 = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 2
< Both operands to a binary operator are not of the same type!
<   %shr74 = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 3
< Both operands to ICmp instruction are not of the same type!
<   %eq = icmp eq i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 1
< Both operands to a binary operator are not of the same type!
<   %shr = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 2
< Both operands to a binary operator are not of the same type!
<   %shr54 = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 2
< Both operands to a binary operator are not of the same type!
<   %shr59 = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 3
< Both operands to a binary operator are not of the same type!
<   %shr64 = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 3
< Both operands to a binary operator are not of the same type!
<   %shr69 = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 2
< Both operands to a binary operator are not of the same type!
<   %shr74 = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 3
< LLVM Verification Error: Both operands to ICmp instruction are not of the same type!
<   %eq = icmp eq i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 1
< Both operands to a binary operator are not of the same type!
<   %shr = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 2
< Both operands to a binary operator are not of the same type!
<   %shr54 = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 2
< Both operands to a binary operator are not of the same type!
<   %shr59 = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 3
< Both operands to a binary operator are not of the same type!
<   %shr64 = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 3
< Both operands to a binary operator are not of the same type!
<   %shr69 = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 2
< Both operands to a binary operator are not of the same type!
<   %shr74 = ashr i64 ptrtoint (ptr getelementptr ([3 x i32], ptr null, i32 1) to i64), i32 3
< 
---
> qbe:build/out.ssa:578: invalid type for first operand %t57 in ceqw
> QBE failed with code 256
